import React, { Component } from 'react';
import Cards from '../layouts/Cards';

class Home extends Component {
    render() {
        return (
            <div>
                <Cards/>
            </div>
        )
    }
}

export default Home;
